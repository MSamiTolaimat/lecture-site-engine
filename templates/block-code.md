#### 💻 الكود: {title}

#### ما هذا الكود؟
> [ماذا يفعل؟ لماذا وُجد؟]

```{language}
// English comment per line
{code}
```

#### شرح كل سطر:
1. `{line}` → {role} — {why}

**المكتبات المطلوبة (Imports):**
> `import ...`

**الناتج المتوقع (لقطة الشاشة):**
> [وصف المخرجات]
